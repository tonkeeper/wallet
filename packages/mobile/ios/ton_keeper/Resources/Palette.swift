import UIKit

enum Palette {
  
  // MARK: - Font
  static let font = R.font.montserratRegular(size: 16.0) ?? .systemFont(ofSize: 16.0, weight: .regular)
  static let fontSemibold = R.font.montserratSemiBold(size: 16.0) ?? .systemFont(ofSize: 16.0, weight: .semibold)
  static let fontBold = R.font.montserratBold(size: 16.0) ?? .systemFont(ofSize: 16.0, weight: .bold)
  static let fontMedium = R.font.montserratMedium(size: 16.0) ?? .systemFont(ofSize: 16.0, weight: .medium)
  
}

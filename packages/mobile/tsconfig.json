{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "useUnknownInCatchVariables": false,
    "allowJs": true,
    "skipLibCheck": false,
    "strict": true,
    "noImplicitAny": false,
    "baseUrl": "src",
    "paths": {
      "$api": ["api"],
      "$core": ["core"],
      "$modals": ["modals"],
      "$store": ["store"],
      "$uikit": ["uikit"],
      "$utils": ["utils"],
      "$hooks": ["hooks"],
      "$libs": ["libs"],
      "$styled": ["styled"],
      "$styles": ["styles"],
      "$shared": ["shared"],
      "$assets": ["assets"],
      "$navigation": ["navigation"],
      "$wallet": ["wallet"],
      "$logger": ["logger"],
      "$translation": ["translation"],
      "$tonconnect": ["tonconnect"],
      "$api/*": ["api/*"],
      "$core/*": ["core/*"],
      "$modals/*": ["modals/*"],
      "$store/*": ["store/*"],
      "$uikit/*": ["uikit/*"],
      "$utils/*": ["utils/*"],
      "$hooks/*": ["hooks/*"],
      "$libs/*": ["libs/*"],
      "$styled/*": ["styled/*"],
      "$styles/*": ["styles/*"],
      "$shared/*": ["shared/*"],
      "$assets/*": ["assets/*"],
      "$navigation/*": ["navigation/*"],
      "$wallet/*": ["wallet/*"],
      "$logger/*": ["logger/*"],
      "$translation/*": ["translation/"],
      "$blockchain": ["blockchain/"],
      "$database": ["database/"],
      "$tonconnect/*": ["tonconnect/*"],
      "$config": ["config"],
      "$components": ["components"],
    }
  },
  "include": ["src", "src/wallet/managers", "src/wallet/Wallet"],
  "exclude": ["node_modules"]
}